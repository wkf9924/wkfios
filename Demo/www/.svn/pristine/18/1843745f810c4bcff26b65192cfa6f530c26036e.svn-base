define(function () {
    'use strict';
    function ctrl($scope,$state,$ionicHistory) {
        $state.go('address-my');
        $scope.goBack = function () {
            $ionicHistory.goBack();
        }


    }
    ctrl.$inject = ['$scope','$state','$ionicHistory'];
    return ctrl;
});